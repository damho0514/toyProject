import { Menu } from 'antd';
import styled from 'styled-components';

export const StyledWrapper = styled.div`
  min-height: calc(100vh - 50px);
  width: 160px;
`;

export const CustomSider = styled(Menu)``;
