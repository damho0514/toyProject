import { Button } from '@/components/Button';

type DeleteCommentProps = {
  id: string;
  discussionId: string;
};

export default function DeleteComment() {
  return (
    <div>
      <Button />
    </div>
  );
}
